{%- if include.data -%}
    {% assign data = page[include.data] %}
{% else %}
    {% assign data = page.headline %}
{% endif %}

{%- if data.animated -%}
<section class="headline" data-aos="zoom-out" data-aos-offset="300" data-aos-duration="1000" data-aos-once="true">
{% else %}
<section class="headline">
{% endif %}
    <div class="headline__content">
        {%- if data.title -%}<h3 class="headline__title">{{ data.title }}</h3>{% endif %}
        {%- if data.text -%}{{ data.text | markdownify }}{% endif %}
        {%- if data.buttons -%}
            <div class="headline__buttons">
                {% for button in data.buttons %}
                    {% assign href_in = button.url %}{% include parts/href.liquid %}<a href="{% if href_out contains '://' or href_out contains '#' %}{{ href_out }}{% else %}{{ href_out | relative_url }}{% endif %}" class="btn {{ button.class }}" target="{{ button.target }}">{{ button.title }}</a>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</section>